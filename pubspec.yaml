name: pwd_reservation_app
description: "Bus Reservation App dedicated for PWDs."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  provider: ^6.0.0
  flutter_secure_storage: ^5.0.2
  camera: ^0.10.5+9
  path_provider: ^2.1.2
  path: ^1.9.0
  http_interceptor: ^2.0.0-beta.8
  image: ^3.2.2
  carousel_slider: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  assets:
    - assets/images/
  uses-material-design: true
